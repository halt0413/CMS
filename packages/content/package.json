{
  "name": "@repo/content",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "pnpm --filter @repo/types build && pnpm --filter @repo/utils build && tsc -p tsconfig.json",
    "typecheck": "pnpm --filter @repo/types build && pnpm --filter @repo/utils build && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@repo/types": "workspace:*",
    "@repo/utils": "workspace:*",
    "zod": "^3.24.1"
  }
}
